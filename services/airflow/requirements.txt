# Airflow project
# ⚠️ Airflow itself installed via constraints - see README.md
# apache-airflow==2.8.0 (DO NOT add here - install separately with constraints)

# Airflow version constraints (prevent pip from pulling 3.x)
apache-airflow==2.8.0

# Airflow providers
apache-airflow-providers-apache-spark==4.1.5

# PySpark (official: Python 3.8, 3.9, 3.10, 3.11)
pyspark==3.5.0

# ML
mlflow==2.14.0

# Data processing
pandas==2.2.2
numpy==1.26.4

# Database (Airflow 2.8 requires SQLAlchemy <2.0)
sqlalchemy>=1.4,<2.0
alembic>=1.13.1,<2.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Feature store from Git
git+https://github.com/acourreg/patternalarm.git@main#subdirectory=feature-store